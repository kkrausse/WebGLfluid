done - Refactor to separate out boundary setting.

done - Set the rest of the boundaries to zero, execpt for the left and right ones,
which are the wind tunnel ones.

done - Separate out all the shape stuff to a different file.

kinda done - Add the boundary stuff for movement to make the movement of the object less bad.

done - Add functions to change params in each thing for the simulation or for the
	boundaries. Maybe add a demo object that combines the boundaries and demo.

done - Add controls for params

done - Write a little tidbit about it and reference that research paper about it.

- Add control to shrink / enlarge shape size

- Add control to change shape
